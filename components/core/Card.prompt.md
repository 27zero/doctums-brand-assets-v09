Editorial content container — flat with a hairline border at rest, no shadow. Pass `interactive` for linked cards: hover adds a soft tactile shadow and a -2px lift.

```jsx
<Card>
  <h3 className="dt-h4">Governance review</h3>
  <p className="dt-body-sm">A structured audit of academic operations.</p>
</Card>
<Card interactive as="a" href="#">…</Card>
```
