Filter/category tag — hairline chip, square-ish radius. Marine-Blue when `active`; pass `onRemove` for a dismiss ×.

```jsx
<Tag active>Strategy</Tag>
<Tag onRemove={() => drop('k12')}>K-12</Tag>
```
