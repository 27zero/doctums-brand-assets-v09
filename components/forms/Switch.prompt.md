Toggle switch with a square-ish (2px) track — keeps the flat, architectural feel rather than a rounded pill. Controlled or uncontrolled.

```jsx
<Switch label="Notify the cohort" defaultChecked />
```
