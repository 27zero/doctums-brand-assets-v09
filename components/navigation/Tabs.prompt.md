Editorial underline tabs — uppercase Montserrat labels on a hairline rule; the active tab carries a Marine-Blue underline. Controlled or uncontrolled.

```jsx
<Tabs
  tabs={[{id:'overview',label:'Overview'},{id:'modules',label:'Modules'}]}
  defaultValue="overview"
  onChange={(id) => setView(id)}
/>
```
